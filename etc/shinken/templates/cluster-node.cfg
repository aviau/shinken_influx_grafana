define host{
    name                     cluster-node
    use                      generic-host
    register                 0
}

define service{
    host_name                cluster-node
    service_description      docker-daemon
    use                      generic-service
    register                 0
    check_command            check_tcp!4242
}
